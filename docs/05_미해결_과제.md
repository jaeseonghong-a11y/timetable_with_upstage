＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
파일명: 05_미해결_과제.md
역할: 아직 안 풀린 질문·다음 액션·확인 필요 플래그. "다음에 뭐 해야 하지?"의 답.
업데이트 규칙: 과제 해결되면 [해결]로 표시하고 결론을 02/03/04 해당 파일에 옮긴 뒤,
              여기선 [해결→02참조]처럼 짧게 남기거나 삭제. 새 과제는 우선순위 매겨 추가.
최종 갱신: 2026-07-13
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

═══════════════════════════════════════
★ 최우선 (Phase 2 진행 중 재발견)
═══════════════════════════════════════

[P3-a] ★ selectMain.do 요청 바디 인코딩 재검증 필요 (2026-07-13 실측)
- 증상: `scraper/skku_scraper/client.py`로 실제 라이브 호출 시 ErrorCode:int=0(성공)은 받지만
  데이터 행이 0개. "KEY=VALUE"·"KEY:string=VALUE" 두 인코딩 방식 모두 동일 결과.
- 부수 발견(해결됨): 기본 python-requests User-Agent로는 404/커넥션리셋 → 브라우저
  User-Agent·Referer·Origin 헤더를 붙여야 200을 받음. `client.py`에 이미 반영함.
- 원인 추정: 02_기술검증_기록의 "핵심 파라미터" 목록은 실제 캡처의 요약이라, 앞에 있었다는
  "쿠키/GA값들"이나 Dataset 포맷(요청도 응답처럼 `Dataset:name`+`_RowType_`+행 구조일 가능성)
  등 누락된 구조가 있을 수 있음.
- 다음 액션: 방법C(넥사크로 엔진 통신 후킹, 브라우저 네트워크 캡처)를 다시 떠서 실제 바이트를
  재확보해야 함. 이 세션엔 브라우저 자동화 도구가 없어 재현 못 함 — 브라우저 MCP 있는 세션에서
  진행 필요. 그 전까지 프로덕션 서버에 무의미한 반복호출 자제 (요청 간격 원칙 유지).
- 영향: Phase 2 완료를 막는 블로커. ssv.py 파서 자체는 실제 응답으로 검증 완료(컬럼명 타입
  접미사 스트립 등 반영됨) — 문제는 요청 인코딩 쪽에 한정됨.

═══════════════════════════════════════
[해결됨] P0 — 개발 착수 전 최우선 과제
═══════════════════════════════════════

[해결→01 D-12] Upstage 필연성 방향 결정 → 옵션A(강의계획서 파싱 중심 재설계) 채택.
[해결→01 D-12] 정량지표 정의 → "시간제약 + Upstage파생 부담도(과제비중·시험집중도·수업형태)".
  평점은 v1 스코프 제외(아래 P1 참조).

═══════════════════════════════════════
높음 (개발 초기 확인)
═══════════════════════════════════════

[해결→01 D-12] P1 평점 데이터 소스 확정 → v1은 평점 제외(옵션C 채택). 데모 D-12로 시간 빠듯해
  공시자료 확인/사용자입력 UI는 우선순위 낮음. 평점은 v2 확장 후보로 보존.

[P2] Upstage 콘솔 실검증 (레퍼런스상 [불명확] 항목들) — ★ 개발계획 Phase 3 착수 시 최우선 처리
- 챗봇 엔드포인트 /v1/chat/completions 실재 여부 (노션엔 명시, 교안엔 불명확 — 상충)
- Document Parse 실제 성능: 강의계획서 PDF 1건 돌려서 필드추출 품질 확인
- Parse/Extract 요청 파라미터·응답 스키마 (레퍼런스 [불명확])
- Solar 모델명(solar-pro3) 및 설정
- → 구현 직전 console.upstage.ai에서 재확인 (규칙 04-5 민감항목).

[P3] 수집방법 A안 개발 전 세부 확인 — ★ 전부 해결됨 (2026-07-11). 단 실제 구현 중 P3-a 재발견(위 참조)
- [해결됨] A/B/C 수집방법 검증 완료. A안 확정. (→ 02_기술검증_기록, D-11)
- [해결됨] 쿠키 필요 여부 → 불필요 확정 (credentials:omit로도 성공). (→ 02 추가검증)
- [해결됨] TERM 코드 매핑 → 10=1학기/15=여름/20=2학기/25=겨울(미개설). (→ 02 추가검증)
- [해결됨] 교양(selectMain03.do) 직접호출 검증 → 쿠키없이 성공, 스키마 전공과 공통. (→ 02 추가검증)
- [해결됨] HAKGWA_CD 학과코드 전체 목록 → selectBizType04.do로 132개 수집 완료.
  결과: "성대_학과코드_전체.txt" 파일. (→ 02_기술검증_기록)
- ⇒ 데이터 수집 파이프라인 관련 확인은 전부 종료. 이제 실제 수집기 개발만 남음.

═══════════════════════════════════════
중간 (설계 단계)
═══════════════════════════════════════

[P4] 경우의수 폭발 대응 로직 설계 — 개발계획 Phase 4(조합로직)에서 처리
- 제약(공강요일·1교시회피 등) + 목적함수(부담도최소/이동최소) → 상위 N개 필터.
- 이게 서비스 차별점. 필터·페이징 처음부터 설계 (단국대 ~1만개 교훈).

[P5] 교양 2단계 조회 정밀 확인 — 개발계획 Phase 2(수집기)에서 처리
- 교양은 영역버튼 클릭→과목 뜨는 방식. 정확한 수집 흐름 확인 필요.

[P6] 강의계획서 PDF 실제 확보 경로 — ★ 개발계획 Phase 3 착수 시 최우선 처리
- selectMain03.do 응답에 lcms.skku.edu 계획서 URL 있음 → 이게 로그인 없이 열리는지 확인.
- 열리면 자동 Parse 파이프라인, 안 열리면 사용자 업로드.

═══════════════════════════════════════
낮음 (나중)
═══════════════════════════════════════
[P7] 성대 이용약관에 크롤링 금지 문구 있는지 (배포·확장 시)
[P8] 배포 방식(Vercel) + 메타태그/OG/파비콘/SEO
[P9] 팀 역할분담 (수집/파싱/프론트/발표)

═══════════════════════════════════════
[해결됨] — 결론은 각 파일로 이관
═══════════════════════════════════════
[해결→02] 성대 개설강좌 API 존재·로그인불필요·필드 → 02_기술검증_기록 1번
[해결→02] robots.txt 확인 → 404, 차단없음
[해결→02] 메뉴별 조회조건 상이 확인 → 02_기술검증_기록 1번
[해결→01] 서비스 범위(성대한정/전공+교양) → D-06, D-07
[해결→02] 단국대 선례 분석 → 02_기술검증_기록 3번
[해결→02] 에브리타임 데이터확보 방식 → 02_기술검증_기록 4번
